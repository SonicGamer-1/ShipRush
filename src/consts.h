#pragma once

const inline int PLAYER_SIZE = 64;

const inline int WIN_H = 900;
const inline int WIN_W = 1600;

const inline float ACCEL = 1000.0f;
const inline float ACCEL_BOOST = 100.0f;
const inline float BULLET_SPEED = 2000.0f;